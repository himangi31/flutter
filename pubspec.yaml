name: ios_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # Dart 2.19.x compatible

dependencies:
  flutter:
    sdk: flutter

  # App dependencies
  http: ^1.2.0
  fl_chart: ^0.68.0
  dio: ^5.0.0
  excel: ^2.1.0
  path_provider: ^2.0.11
  permission_handler: ^10.4.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.1.1
  share_plus: ^6.3.0
  image_picker: ^0.8.7+4
  google_mlkit_text_recognition: ^0.7.0  # Old version compatible with Dart 2.x

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # ✅ Only one place, Dart 2.19.x compatible

flutter:
  uses-material-design: true

  assets:
    - assets/
